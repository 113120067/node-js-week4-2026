# 第四週作業繳交筆記

這份筆記整理的是這次 Week 4 作業的實作思路、三個主要檔案的寫法，以及最後的驗證方式。我的做法是先依照 Swagger 的規格確認 API 行為，再把驗證、註冊、登入、個人資料與 app 組裝分開處理，最後用 Jest 驗證整體結果。

## 作業目標

這份作業的核心是把 middleware、bcrypt、JWT 整合成一個完整的登入系統。需要完成的功能有三個重點：

1. 註冊管理員帳號
2. 登入後簽發 JWT token
3. 使用 token 取得自己的資料

除了這三支 API，還要補上 404 與錯誤處理 middleware，讓整個 Express app 的行為完整。

## 我的實作順序

我沒有直接從路由開始寫，而是先完成會被重複使用的 verifyToken，再補 auth router，最後才組裝 app.js。這樣做的原因是：

- verifyToken 是 `/auth/me` 的前置條件
- register 和 login 都要共用 users 狀態與 JWT 流程
- app.js 是最後的拼裝層，最適合在功能都寫好後一次掛上 middleware

## Step 3 之後怎麼接

我在 Step 3 已經先確認 Swagger UI 的規格，所以後續就是照 Swagger 的 request / response 去實作。實作時我會特別注意三件事：

- 每個 endpoint 的狀態碼是否正確
- 錯誤訊息是否符合測試期待
- middleware 的順序有沒有放對

## JS 語法快速對照表

這一段是給剛開始看 JS 的自己看的。先不用一次記住全部，先把「這個語法在做什麼」看懂就好。

### 1. `const` 和 `let`

`const` 用來宣告不打算重新指定的變數；`let` 用來宣告之後可能會改值的變數。

在這份作業裡：

- `const users = ...` 表示 users 這個名稱不會換掉
- `let nextId = ...` 表示下一個 id 之後會增加

### 2. `function`

`function` 是在定義一段可重複使用的程式。

這份作業裡的 `verifyToken` 就是一個 function，意思是「當這個 middleware 被呼叫時，就執行裡面的檢查流程」。

### 3. `async` / `await`

`async` 代表這個函式裡會用到非同步操作；`await` 代表「先等這件事做完，再往下走」。

像 `bcrypt.hash`、`bcrypt.compare`、`jwt.sign` 這類操作，常常都需要搭配 `await`，不然拿到的可能不是結果，而是 Promise。

### 4. `if`

`if` 是條件判斷。

這份作業中最常見的句型是：

- 如果沒有帶資料，就回 400 或 401
- 如果 email 已存在，就不能註冊
- 如果密碼錯誤，就不能登入

### 5. 陣列方法 `find` 和 `some`

這兩個方法都很常用在找資料。

- `find`：找出第一個符合條件的物件
- `some`：只檢查「有沒有任何一筆符合」

這份作業裡：

- `find` 用在登入時找 email 對應的使用者
- `some` 用在註冊時檢查 email 有沒有重複

### 6. `try/catch`

`try/catch` 是用來處理可能會出錯的程式。

如果 `try` 裡面出錯，就會跳到 `catch`。這份作業裡 token 驗證就是典型例子，因為 token 無效、格式錯誤、過期都可能丟例外。

## verifyToken 逐行白話講解

這一段是把 [middlewares/verifyToken.js](middlewares/verifyToken.js) 拆開來看，讓每一行都知道在做什麼。

### 1. 先載入 jwt

第一行是 `const jwt = require('jsonwebtoken');`。

白話來說就是「把驗證 JWT 的工具拿進來用」。如果沒有這一行，後面就沒辦法呼叫 `jwt.verify`。

### 2. 定義 verifyToken 這個中介層

接著是 `const verifyToken = function (req, res, next) { ... }`。

白話來說就是「建立一個可以插在路由前面的檢查關卡」。

- `req`：這次請求送進來的資料
- `res`：要回給前端的資料
- `next`：通過檢查後，往下一個處理流程走

### 3. 讀取 Authorization header

`const authorization = req.headers.authorization;`

這一行是在從請求標頭裡面，把 `Authorization` 拿出來。因為 token 會放在這裡，所以第一步一定要先讀它。

### 4. 檢查格式是不是 Bearer token

`if (!authorization || !authorization.startsWith('Bearer ')) { ... }`

這一段是在判斷兩件事：

- 有沒有帶 Authorization
- 格式是不是 `Bearer <token>`

如果沒有，就直接回 401，意思是「你還沒登入」。

### 5. 取出真正的 token

`const token = authorization.slice(7);`

這一行是在把 `Bearer ` 前綴切掉，只保留真正的 token。

因為 `Bearer ` 一共有 7 個字元，所以用 `slice(7)` 就可以直接拿到後面的 token。

### 6. 驗證 token

`const decoded = jwt.verify(token, process.env.JWT_SECRET);`

這一行是在確認 token 是不是合法的。

白話來說就是：

- token 是不是這個網站簽出來的
- token 有沒有過期
- token 裡面的資料可不可信

如果驗證成功，`decoded` 就會是解出來的使用者資料。

### 7. 把 decoded 放進 req.user

`req.user = decoded;`

這一行是在把驗證過的使用者資料掛回 request 上。這樣後面的 `/auth/me` 才可以直接讀 `req.user`。

### 8. 呼叫 next()

`return next();`

這一行很重要，因為它代表「驗證通過，繼續往下執行後面的路由」。如果少了這行，請求就會卡住。

### 9. catch 區塊處理失敗情況

如果 `jwt.verify` 出錯，就會進到 `catch`。

這時候回傳 401 和「Token 無效或已過期」的訊息，表示使用者帶來的 token 不可用。

### verifyToken 的一句話理解

這個 middleware 的流程可以記成：先看有沒有帶 token，再驗 token，驗過就放行，失敗就擋下來。

## 檔案一：middlewares/verifyToken.js

這個檔案負責 JWT 驗證，也就是登入保護的第一道防線。

### 檔案角色

它的工作是檢查 request header 裡有沒有帶 `Authorization: Bearer <token>`，如果有就驗證 token，如果沒有或 token 無效，就直接回 401。

### 寫法說明

第一段先讀 `req.headers.authorization`。這一段是在確認使用者有沒有真的帶 token 進來。若 header 不存在，或格式不是 Bearer 開頭，就直接回傳 401，避免後面繼續處理。

第二段會把 Bearer 後面的 token 取出來。因為 header 的格式是 `Bearer xxx`，所以 token 需要切掉前面的字串再交給 jwt 驗證。

第三段使用 `jwt.verify` 搭配 `process.env.JWT_SECRET` 來驗證 token。這裡我用 try/catch 包住，因為 token 過期或格式錯誤時，jwt.verify 會丟例外。

第四段是在驗證成功後把 decoded payload 放到 `req.user`，這樣後面的路由就可以直接讀使用者資訊。最後一定要呼叫 `next()`，不然請求會卡住。

### 這個檔案的重點

- 先檢查 header 格式，再做 token 驗證
- 錯誤時統一回 401
- 成功後把資料放進 `req.user`

## 檔案二：routes/auth.js

這個檔案是這次作業的主要商業邏輯區，包含註冊、登入與取得個人資料。

### 檔案角色

我用一個 module 層級的 users 陣列當作簡易資料庫，並複製 fixtures/users.json 進來。這樣做的好處是測試時每次重新載入 app，資料都會回到乾淨狀態。

### 共用 state 的寫法

一開始先把 initialUsers 複製成 users，再準備 nextId 作為新增使用者的 id。這樣 register 時可以把新使用者推進陣列，而不會改到原始 fixtures。

### register 的寫法

第一段先從 req.body 拿到 email 和 password，然後檢查欄位是否都存在。只要缺少任何一個欄位，就回 400。

第二段會用陣列方法檢查 email 是否已經存在。這是註冊最基本的重複檢查，如果已存在就回 400。

第三段使用 bcrypt 產生 salt，再把密碼 hash 起來。這裡一定要記得 await，因為 bcrypt 的操作是非同步。

第四段建立新的 user 物件，內容包含 id、email、hash 後的 password，再把它存進 users 陣列中，同時把 nextId 遞增。

第五段回傳 201 和成功訊息，表示註冊完成。

### login 的寫法

第一段先根據 email 在 users 陣列找出對應帳號。找不到時直接回 401，而且訊息要跟密碼錯誤一致，這樣比較不會讓人從回應判斷帳號是否存在。

第二段用 bcrypt.compare 比對明文密碼與資料庫中的 hash。只要比對失敗，一樣回 401 與相同錯誤訊息。

第三段在登入成功後用 jwt.sign 簽出 token。payload 帶入 id 和 email，secret 用 JWT_SECRET，並設定 expiresIn 為 30d。

第四段回傳 200 與 token。這樣前端或 Swagger 就可以拿這個 token 去呼叫 /auth/me。

### me 的寫法

這支路由前面直接掛 verifyToken，表示只有驗證通過才會往下執行。

驗證通過後，req.user 會有 decoded payload，所以這支路由只要直接把 req.user 回傳給使用者即可。這樣就完成「取得自己資料」的功能。

### 這個檔案的重點

- register 要檢查欄位與 email 重複
- login 要統一失敗訊息，避免帳號探測
- me 要依賴 verifyToken

## 檔案三：app.js

這個檔案是整個 Express 應用的組裝中心，負責把 middleware 和 router 串起來。

### 檔案角色

app.js 不負責啟動 server，啟動是交給 server.js。app.js 的工作只有「組裝」。這樣測試時可以直接載入 app，不需要真的開 port。

### middleware 的順序

我在這個檔案中依序掛上：

1. cors()
2. express.json()
3. /docs Swagger UI
4. /auth router
5. 404 middleware
6. 錯誤處理 middleware

### 寫法說明

第一段是 cors。這會讓測試與前端請求有基本的跨域 header。

第二段是 express.json。這非常重要，因為 register 和 login 都需要解析 JSON body，而且壞掉的 JSON 也會在這裡被丟進後面的 error handler。

第三段是 Swagger UI。這部分已經由題目先提供，我只要照著掛上 `/docs` 就好。

第四段是掛上 `/auth` router。這樣所有 auth 相關 API 都會集中在同一個路徑底下。

第五段是 404 middleware。當路由不存在時，回傳 404 和無此路由訊息。

第六段是錯誤處理 middleware，而且一定要是四個參數。它負責接住 express.json 在解析壞 JSON 時丟出的 SyntaxError，並統一回傳 500 與錯誤資訊。

### 這個檔案的重點

- middleware 順序不能錯
- 404 要放在所有路由後面
- error handler 一定要四個參數，而且要放最後
- app.js 不要呼叫 app.listen

## 驗證方式

完成後我先跑 `npm test`，確認 16 個測試全部通過。若要手動檢查，也可以先執行 `npm start`，再透過 Swagger UI 到 `http://localhost:3000/docs` 測試 register、login 和 me。

## 我這次的實作心得

這份作業最重要的不是單一函式的寫法，而是把整個請求流程串起來。verifyToken 負責驗證，auth router 負責商業邏輯，app.js 負責組裝。只要三個檔案的責任分清楚，整體就會很穩定。

## 容易錯的地方

- bcrypt 的函式沒有 await
- jwt.verify 成功後忘記把 decoded 放進 req.user
- /auth/me 沒有掛 verifyToken
- express.json 放錯位置，導致壞 JSON 沒有被 error handler 接住
- app.js 少了 404 或 error handler

## 結論

我這次是依照 Swagger 規格與測試需求完成整份作業，並且把 middleware、JWT、bcrypt 與路由組裝在一起。最後測試結果為 16 項全數通過，表示功能與回傳格式都符合題目要求。
